# Installation 
This installation assumes that you have already installed PostGIS, which is done during the [MobilityDB installation](../../mobilitydb/single/).  
Afterwards, simply execute the shell script on the machine (root priviledges might be required) 

```
./setup.sh
```
